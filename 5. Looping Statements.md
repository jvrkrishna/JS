# 📘 Loops in JavaScript

# 🌟 What is a Loop?
A **loop** is used to execute a block of code repeatedly until a specified condition becomes **false**.

Instead of writing the same code multiple times, we use loops to make our programs shorter and more efficient.

### Without a Loop
```javascript
console.log("Hello");
console.log("Hello");
console.log("Hello");
```

### With a Loop
```javascript
for (let i = 1; i <= 3; i++) {
    console.log("Hello");
}
```

# 📂 Types of Loops in JavaScript
JavaScript provides the following loops:

1. `for` loop
2. `while` loop
3. `do...while` loop

# 🔁 1️⃣ `for` Loop
The `for` loop is used when the **number of iterations is known**.

## Syntax
```javascript
for (initialization; condition; increment/decrement) {
    // Code to execute
}
```

### Parts of a `for` Loop

- **Initialization** → Executes only once before the loop starts.
- **Condition** → Checked before every iteration.
- **Increment/Decrement** → Updates the loop variable after each iteration.

## Example
```javascript
for (let i = 1; i <= 5; i++) {
    console.log(i);
}
```

### Output
```
1
2
3
4
5
```

# 🔁 2️⃣ `while` Loop
The `while` loop is used when the **number of iterations is not known in advance**.

The loop continues as long as the condition is `true`.

## Syntax
```javascript
while (condition) {
    // Code to execute
}
```

## Example
```javascript
let i = 1;

while (i <= 5) {
    console.log(i);
    i++;
}
```

### Output
```
1
2
3
4
5
```

# 🔁 3️⃣ `do...while` Loop
The `do...while` loop executes the code **at least once**, even if the condition is `false`.

## Syntax
```javascript
do {
    // Code to execute
} while (condition);
```

## Example
```javascript
let i = 1;

do {
    console.log(i);
    i++;
} while (i <= 5);
```

### Output
```
1
2
3
4
5
```

# 📌 Difference Between `while` and `do...while`

| `while` Loop | `do...while` Loop |
|--------------|-------------------|
| Checks the condition first | Executes the code first |
| May execute zero times | Executes at least one time |

## Example

### `while` Loop
```javascript
let i = 10;

while (i <= 5) {
    console.log(i);
}
```

Output:
```
No Output
```

### `do...while` Loop
```javascript
let i = 10;

do {
    console.log(i);
} while (i <= 5);
```

Output:
```
10
```

# 🔀 Loop Control Statements

## `break` Statement
The `break` statement immediately terminates the loop.

## Example
```javascript
for (let i = 1; i <= 5; i++) {
    if (i === 3) {
        break;
    }

    console.log(i);
}
```

### Output
```
1
2
```

# `continue` Statement
The `continue` statement skips the current iteration and continues with the next iteration.

## Example
```javascript
for (let i = 1; i <= 5; i++) {
    if (i === 3) {
        continue;
    }

    console.log(i);
}
```

### Output
```
1
2
4
5
```

# ⚠️ Infinite Loop
An infinite loop occurs when the loop condition never becomes `false`.

## Example
```javascript
let i = 1;

while (i <= 5) {
    console.log(i);
}
```

Since `i` is never incremented, the loop runs forever.

> **Note:** Always make sure the loop condition eventually becomes `false`.

# 📌 Nested Loops
A nested loop is a loop inside another loop.

## Example
```javascript
for (let i = 1; i <= 3; i++) {
    for (let j = 1; j <= 2; j++) {
        console.log(i, j);
    }
}
```

### Output
```
1 1
1 2
2 1
2 2
3 1
3 2
```

# ✍️ Practice

## Practice 1
Print numbers from 1 to 10.

```javascript
for (let i = 1; i <= 10; i++) {
    console.log(i);
}
```

## Practice 2
Print even numbers from 1 to 20.

```javascript
for (let i = 1; i <= 20; i++) {
    if (i % 2 === 0) {
        console.log(i);
    }
}
```

## Practice 3
Print numbers using a `while` loop.

```javascript
let i = 1;

while (i <= 5) {
    console.log(i);
    i++;
}
```

## Practice 4
Print numbers using a `do...while` loop.

```javascript
let i = 1;

do {
    console.log(i);
    i++;
} while (i <= 5);
```

## Practice 5
Stop a loop using `break`.

```javascript
for (let i = 1; i <= 10; i++) {
    if (i === 6) {
        break;
    }

    console.log(i);
}
```

## Practice 6
Skip even numbers using `continue`.

```javascript
for (let i = 1; i <= 10; i++) {
    if (i % 2 === 0) {
        continue;
    }

    console.log(i);
}
```